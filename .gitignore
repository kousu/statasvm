# hide backup files
*~

# hide OS X cruft
.DS_Store

#hide paper
/paper
/paper.nick

# avoid committing build products //comment out at July 2018 since we added seed and need the plugin files to build cross platform distribution file
#*.plugin

*.o
*.obj

#note: some dlls might end up getting committed (git add -f!), at least temporarily, just to make progress on OS X and Windows faster
# in the long run, we should be dynamically building or at least retrieving libraries, not keeping personal copies around.
*.so
*.dylib
*.dll

*.lib
*.exp

*.log

# hide private files (copyrighted journal articles for reference, etc)
reference/
software/
devlog/
VMs/

remotes/
