*~

*.o
*.plugin

#note: some dlls might end up getting committed (git add -f!), at least temporarily, just to make progress on OS X and Windows faster
# in the long run, we should be dynamically building or at least retrieving libraries, not keeping personal copies around.
*.so
*.dll

deps/
software/
devlog/
VMs/
